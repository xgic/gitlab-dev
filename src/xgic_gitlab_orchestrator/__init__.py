"""XGIC GitLab production orchestration helpers (scaffold).

Runtime configuration (hosts, tokens, project paths) must come from
environment or config files — never hard-coded private production values.
"""

__version__ = "0.1.0.dev0"
